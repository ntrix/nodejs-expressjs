extends ../common
block header
  h1 Library
  h3 Edit book's information &nbsp;
  a.btn.btn-secondary(href='/books') Back to book management
block content
  form(action='/books/upd' method='post')
    input(type='text' name='id' value=chosenBook.id hidden)
    div
      label Title: 
      div
        input(type='text' name='title' value=chosenBook.title required)
    div
      label Description:
      div
        input(type='text' name='description' value=chosenBook.description)
    button.mt-3.btn.btn-primary(type='submit') Update this book
  h3 Book list
  ul
    each book in books
      li
        div
          b #{book.title} &nbsp;
          p Description: #{book.description}