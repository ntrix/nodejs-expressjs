h1 User management
h2 Edit user's information
form(action='/books/upd' method='post')
  input(type='text' name='id' value=chosenUser.id hidden)
  div
    label Username: 
    div
      input(type='text' name='username' value=chosenUser.username required)
  div
    label Email:
    div
      input(type='email' name='email' value=chosenUser.email)
  button(type='submit') Update this user
a(href='/books')
  button Back to user management
h3 User list
ul
  each user in users
    li
      div
        b #{user.username} &nbsp;
        a(href='/users/del/' + user.id)
            button Delete
        p Email: #{user.email}